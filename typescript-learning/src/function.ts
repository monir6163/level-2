function addNormal(num1: number, num2: number): number {
  return num1 + num2;
}
console.log(addNormal(2, 8));

const arrowFun = (num1: number, num2: number): number => num1 + num2;
console.log(arrowFun(5, 10));

const objFun = {};
